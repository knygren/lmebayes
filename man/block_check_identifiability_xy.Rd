% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lmbBlock.R
\name{block_check_identifiability_xy}
\alias{block_check_identifiability_xy}
\title{Check block-level and hyper-level identifiability (matrix interface)}
\usage{
block_check_identifiability_xy(
  x,
  block,
  X_nbhd = NULL,
  on_failure = c("warn", "stop")
)
}
\arguments{
\item{x}{Numeric design matrix \code{(l2 x l1)}.}

\item{block}{Block specification: factor, integer/character vector, or list
of row-index vectors.  Passed to \code{\link[glmbayesCore]{normalize_block}()}.}

\item{X_nbhd}{Optional \code{(k x q)} numeric matrix of group-level
covariates (one row per block, in block-id order or with matching
\code{rownames}).  \code{NULL} assumes an intercept-only hyper design.}

\item{on_failure}{One of \code{"warn"} (default) or \code{"stop"}.}
}
\value{
Invisibly, the same list structure as
\code{block_check_identifiability()}.
}
\description{
Matrix-interface variant of \code{block_check_identifiability()} for
use when the design matrix \code{x} and response \code{y} are already formed
(e.g. inside a Gibbs loop setup).  Applies the same two-level algorithm:

\enumerate{
\item \strong{Level 1:} full column rank of \code{x[rows_b, ]} per block.
\item \strong{Level 2:} full column rank of \code{X_nbhd} restricted to
Level-1-identified blocks.
}

See \code{block_check_identifiability()} and
\code{inst/BLOCK_GIBBS_ERGODICITY.md} for background.
}
\seealso{
\code{block_check_identifiability()},
\code{\link{rNormalGLMBlock}}
}
\keyword{internal}
