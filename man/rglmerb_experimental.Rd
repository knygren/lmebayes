% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rglmerb_experimental.R
\name{rglmerb_experimental}
\alias{rglmerb_experimental}
\title{Experimental \code{rGLMM}-based two-stage Gibbs sampler}
\usage{
rglmerb_experimental(
  n,
  design,
  prior,
  family = poisson(),
  fixef_start = NULL,
  m_convergence = NULL,
  n_pilot = NULL,
  m_convergence_pilot = NULL,
  tv_tol = 0.01,
  mode_gap_max = 1,
  seed = NULL,
  collect_block1 = TRUE,
  verbose = TRUE,
  progbar = FALSE
)
}
\arguments{
\item{n}{Integer. Number of independent chains in the main stage.  Each
chain stores one draw after \code{m_convergence} inner Gibbs sweeps.}

\item{design}{A \code{\link{model_setup}} object as returned by
\code{\link{model_setup}}, supplying \code{y}, \code{Z}, \code{groups},
\code{X_hyper}, \code{group_name}, and \code{re_coef_names}.}

\item{prior}{A \code{lmebayes_prior_setup} object as returned by
\code{\link{.lmebayes_priors_from_pfamily_list}}.}

\item{family}{A \code{\link[stats]{family}} object for the response model.
Default \code{poisson()}.}

\item{fixef_start}{Optional named list of starting hyper-parameter vectors
(one per RE component).  When \code{NULL} (default), the ICM posterior
mode is computed internally via
\code{\link[glmbayesCore]{glmerb_posterior_mode}}.}

\item{m_convergence}{Optional integer. Number of inner Gibbs sweeps per
stored main-stage draw.  When \code{NULL} (default), derived from
\code{tv_tol} via Theorem 3 (Nygren 2020) and floored at the derived
\code{m_min}.  A user-supplied value is floored at \code{m_min} with a
warning if it had to be raised.  The final value may be further increased
by the pilot upper-bound step.}

\item{n_pilot}{Integer. Number of independent chains in the pilot stage.
\code{0} or \code{NULL} skips the pilot and starts the main stage from
\code{fixef_start}.  Default \code{NULL}.}

\item{m_convergence_pilot}{Optional integer. Number of inner Gibbs sweeps
per pilot chain.  When \code{NULL} (default) and \code{n_pilot > 0},
derived from \code{mode_gap_max} via Theorem 3; floored at \code{m_min}.
A user-supplied value is used as-is.}

\item{tv_tol}{Single numeric in \code{(0, 1)}. Total variation tolerance
used for convergence calibration.  Default \code{0.01}.}

\item{mode_gap_max}{Single positive numeric. Maximum per-coordinate
mode--mean gap (in posterior SD units) that \code{m_convergence_pilot} is
calibrated to cover.  Default \code{1.0}.  Ignored when
\code{m_convergence_pilot} is user-supplied.}

\item{seed}{Optional integer RNG seed passed to \code{\link{run_short_chains}}.
Default \code{NULL} (no seeding).}

\item{collect_block1}{Logical. If \code{TRUE}, collect and row-bind the
Block 1 (\code{coefficients}) matrix from every main-stage chain.
Default \code{TRUE}.}

\item{verbose}{Logical. Print stage headers and convergence diagnostics.
Default \code{TRUE}.}

\item{progbar}{Logical. Passed to \code{\link{run_short_chains}}; when
\code{TRUE}, shows chain-level progress bars during pilot and main
sampling. Default \code{FALSE}.}
}
\value{
An object of class \code{c("rglmerb_experimental", "list")} with the
same components as \code{\link{rglmerb}}.
}
\description{
Batch pilot and main sampling via a single call to
\code{\link[glmbayesCore]{rGLMM}}.  Kept for development and comparison;
\code{\link{rglmerb}} uses independent short chains via
\code{\link{run_short_chains}} instead.
}
\seealso{
\code{\link{rglmerb}}, \code{\link{glmerb}}, \code{\link{rlmerb}},
\code{\link[glmbayesCore]{rGLMM}}
}
