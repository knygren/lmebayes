% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/two_block_rNormal_reg.R
\name{two_block_rNormal_reg}
\alias{two_block_rNormal_reg}
\title{Two-block Gaussian Gibbs sampler for hierarchical regression}
\usage{
two_block_rNormal_reg(
  n,
  y,
  x,
  block,
  x_hyper,
  prior_list_block1,
  prior_list_block2,
  fixef_start,
  re_coef_names = colnames(x),
  group_levels = levels(block),
  group_name = NULL,
  m_convergence = 10L,
  sampling = c("replicate", "chain"),
  seed = NULL,
  progbar = TRUE
)
}
\arguments{
\item{n}{Number of stored draws.}

\item{y}{Response vector of length \code{nrow(x)}.}

\item{x}{Level-1 design matrix \code{Z} (\code{l2 x p_re}).}

\item{block}{Grouping factor or block partition of length \code{l2}.}

\item{x_hyper}{Named list of group-level design matrices \code{X_k}
(\code{J x q_k}), one per column of \code{x}.  Supply
\code{design$X_hyper} from \code{\link{model_setup}}.}

\item{prior_list_block1}{Prior for Block~1: must contain \code{P} or
\code{Sigma}, \code{dispersion}, and optionally \code{ddef}.  The
\code{mu} field (\code{p_re x J}) is updated internally from
\code{fixef} at each Gibbs step.}

\item{prior_list_block2}{Named list of Block~2 prior lists (one per RE
column), each with \code{mu}, \code{Sigma} or \code{P}, and
\code{dispersion}.  Passed to \code{\link{multi_rNormal_reg}}.}

\item{fixef_start}{Named list of hyper-parameter vectors \eqn{\gamma_k}
at which each inner chain is initialised.}

\item{re_coef_names}{Character vector naming columns of \code{x}.  Defaults
to \code{colnames(x)}.}

\item{group_levels}{Character vector defining row order of Block~1 draws
and column order of \code{mu_all}.  Defaults to \code{levels(block)}.}

\item{group_name}{Name for the grouping column in \code{coefficients}.
When \code{NULL}, taken from \code{deparse(substitute(block))} or
\code{"group"}.}

\item{m_convergence}{Number of inner Gibbs steps per stored draw.}

\item{sampling}{Sampling scheme.  Currently only \code{"replicate"} is
implemented.}

\item{seed}{Optional RNG seed set before sampling.}

\item{progbar}{Logical; show a text progress bar over the \code{n} draws.}
}
\value{
Object of class \code{"two_block_rNormal_reg"}: a list with
\code{fixef_draws}, \code{coefficients}, \code{fixef_last},
\code{b_last}, \code{mu_all_last}, and metadata (\code{n},
\code{m_convergence}, \code{sampling}, \code{fixef_start},
\code{re_coef_names}, \code{group_levels}, \code{group_name},
\code{call}).
}
\description{
Runs the coupled Block~1 / Block~2 Gibbs sampler used by
\code{\link{lmerb}}: Block~1 draws group-level random effects \eqn{b_j}
via \code{\link[glmbayesCore]{block_rNormalReg}}; Block~2 updates
hyper means \eqn{\gamma_k} via \code{\link{multi_rNormal_reg}}.
}
\details{
With \code{sampling = "replicate"} (default), each of the \code{n} stored
draws is obtained by resetting to \code{fixef_start}, running
\code{m_convergence} Gibbs steps, and retaining only the final
\eqn{(\gamma, b)} state.  This matches the near-iid sampling strategy
described in \code{\link{lmerb}} (Nygren 2020 TV bound).
}
\seealso{
\code{\link{lmerb}}, \code{\link{build_mu_all}},
\code{\link[glmbayesCore]{block_rNormalReg}},
\code{\link{multi_rNormal_reg}}

glmbayes Simulation Functions
\code{\link{multi_rlmb}()}
}
\concept{simfuncs}
